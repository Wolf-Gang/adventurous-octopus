#include "../backend/dreamland_effects.as"
#include "../backend/emote.as"
#include "../characters/unicorn.as"

[start]
void create_trees()
{
  create_tree(vec(10.5, 5.5));
}

[start]
void start()
{
	//set_position(get_player(), vec(0, 0));
}
